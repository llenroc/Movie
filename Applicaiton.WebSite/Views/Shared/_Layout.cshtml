<!DOCTYPE html>
<html lang="en">
@Html.Partial("_Head")
<body ng-app="app" class="height-full">
    <div class="page" ng-controller="app.home.layout">
        @Html.Action("Header", "Layout", new { currentPageName = ViewBag.CurrentPageName })
        <div class="page-content">
            @RenderBody()
        </div>
        @Html.Partial("footer")
    </div>
</body>
</html>